import os.path
os.path.isfile(fname) 

x = 31
y = 28

faltantes = []

for i in range(x + 1:
	for y in range(y + 1):
		arquivo ="Imagens/%s - %s.png"%(x, y)
		if(!aquivo.isfile()):
			faltantes.add(arquivo)

for f in faltantes:
	print(f)

